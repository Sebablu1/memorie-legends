# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# El lockfile de functions/ SÍ se versiona: lo que hay ahí se despliega a
# producción, y con `^7.3.2` sin lockfile cada deploy resuelve la versión de
# nuevo. Un 7.x publicado entre dos deploys entraría solo, sin que nadie lo
# hubiera pedido ni probado.
/package-lock.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Sistema
.DS_Store
Thumbs.db
*.tmp
*.log

# Environment
.env
.env.local
.env.*.local

# Build
dist/
build/
*.min.js
*.min.css

# Secrets
*.pem
*.key
*.crt

# Firebase Admin
serviceAccountKey.json
*.key.json
# Volcado del código para pegar en el chat. No es parte del sitio.
proyecto_completo.txt
